# Textos legais

Um MDX por documento, com frontmatter `title`, `version` e `date`. O app grava `version` no aceite
(`consents.document_version`); mudar o texto de forma relevante = incrementar `version` e `date`.

Os textos são **rascunhos para revisão jurídica** — não são aconselhamento jurídico.
