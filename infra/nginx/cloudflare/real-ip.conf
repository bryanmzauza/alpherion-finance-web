# Gerado por infra/scripts/update-cloudflare-ips.sh (cron semanal). Não editar à mão.
# Placeholder do repositório: sem as faixas reais, o nginx sobe e o $remote_addr é o IP
# do proxy — o rate limit por IP ficaria errado. Rode o script antes do primeiro `up`.
real_ip_header CF-Connecting-IP;
real_ip_recursive on;
